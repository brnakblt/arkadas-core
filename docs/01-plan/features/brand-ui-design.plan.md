# Plan - Brand UI Design

## 1. Objective
Align the dashboard UI with the project's brand colors (Green and Orange) to make it more "brandish" and consistent with the public website.

## 2. Scope
- Update `DashboardSidebar` component styles.
- Update `DashboardWrapper` and `TopBar` styles.
- Standardize chart colors and widget styles in `Overview`, `StatsWidgets`, etc.
- Enhance consistency across the UI-UX system.

## 3. Tasks
- [ ] Research brand color shades in `tailwind.config.js`.
- [ ] Update `DashboardSidebar.tsx` colors (Sidebar header, active states).
- [ ] Update `TopBar.tsx` styles.
- [ ] Update `Overview.tsx` and `StatsWidgets.tsx` to use brand-consistent accents.
- [ ] Audit other dashboard components for color consistency.

## 4. Verification Plan
- [ ] Visual inspection of the dashboard.
- [ ] Ensure dark mode compatibility.
- [ ] Check accessibility (contrast ratios).
